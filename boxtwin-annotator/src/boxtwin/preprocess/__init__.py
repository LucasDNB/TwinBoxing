"""
Preproceso: pose y tracking sobre todo el video, una vez, a disco.

Depende de core. Arrastra ultralytics y torch, por eso vive fuera de core y se instala
como extra: `pip install -e ".[preprocess]"`.
"""
